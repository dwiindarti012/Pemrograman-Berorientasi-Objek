
class mahasiswa:
    def _init_(self, nama, tanggal_lahir, nim, alamat):
        self.nama = nama
        self.tanggal_lahir = tanggal_lahir
        self.nim = nim
        self.alamat = alamat

    def tampilkanNama(self):
        print("Nama Mahasiswa   :", self.nama)
    def tampilkanTanggallahir(self):
        print("Tanggal lahir    :", self.tanggal_lahir)
    def tampilkanNim(self):
        print("Nim Mahasiswa    :", self.nim)
    def tampilkanAlamat(self):
        print("Alamat           :", self.alamat)

mahasiswa_ti = []


jumlah_mhs_ti = int(input("Masukkan jumlah mahasiswa: "))
for i in range(jumlah_mhs_ti):
    nama = input(f"Masukkan Nama ke-{i+1}: ")
    tanggal_lahir = input(f"Masukkan tanggal lahir ke-{i+1}: ")
    nim = input(f"Masukkan Nim ke-{i+1}: ")
    alamat = input(f"masukkan Alamat ke-{i+1}: ")
    mhs_ti = mahasiswa(nama, tanggal_lahir, nim, alamat)
    mahasiswa_ti.append(mhs_ti)


for mhs in mahasiswa_ti:
    mhs.tampilkanNama()
    mhs.tampilkanTanggallahir()
    mhs.tampilkanNim()
    mhs.tampilkanAlamat()
